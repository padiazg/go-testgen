# GoReleaser configuration for go-testgen
# https://goreleaser.com

version: 2

before:
  hooks:
    # Ensure dependencies are up to date
    - go mod tidy
    - go mod download

builds:
  - main: ./main.go
    binary: go-testgen
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/padiazg/go-testgen/pkg/version.version={{.Version}}
      - -X github.com/padiazg/go-testgen/pkg/version.commit={{.Commit}}
      - -X github.com/padiazg/go-testgen/pkg/version.buildDate={{.Date}}

archives:
  - name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE*
      - CHANGELOG.md

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: 'Bug fixes'
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: 'Enhancements'
      regexp: '^.*?enhance(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: Others
      order: 999

release:
  github:
    owner: padiazg
    name: go-testgen
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## go-testgen {{ .Tag }} Release

    go-testgennal Architecture scaffolding tool for Go projects.

    ### Installation

    Download the appropriate binary for your platform below, or:

    use Go
    ```shell
    go install github.com/padiazg/go-testgen@{{ .Tag }}
    ```
    
    use Homebrew
    ```shell
    brew tap padiazg/go-testgen 
    brew install go-testgen
    ```

  footer: |
    **Full Changelog**: https://github.com/padiazg/go-testgen/compare/{{ .PreviousTag }}...{{ .Tag }}

    ---

    ## Project Types

    - **http-server** - HTTP API with framework support (Echo, Gin, Chi, Fiber, stdlib)
    - **service** - Long-running daemon/service (no web framework for main logic)

    All types support optional observability (health checks + Prometheus metrics).

# Homebrew configuration (GoReleaser v2 format)
# To publish to Homebrew, create a tap repository at github.com/padiazg/homebrew-tap
homebrew_casks:
  - name: go-testgen
    binaries:
      - go-testgen

    repository:
      owner: padiazg
      name: homebrew-go-testgen
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
      
    homepage: https://github.com/padiazg/go-testgen
    description: go-testgennal Architecture scaffolding tool for Go
    license: MIT
    skip_upload: auto
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Cask update for {{ .ProjectName }} version {{ .Tag }}"